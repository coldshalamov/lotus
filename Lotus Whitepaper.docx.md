# Lotus whitepaper draft (archival)

This archival file previously contained manually curated benchmark tables.
Those tables were removed because they were not generated from repository code and could drift from measured artifacts.

Use:

- `docs/WHITEPAPER.md` for maintained design notes,
- `docs/BENCHMARKS.md` for methodology,
- `docs/RESULTS.md` and `docs/results.json` for reproducible data.
