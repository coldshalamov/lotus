[package]
name = "lotus"
version = "0.1.0"
edition = "2024"
authors = ["Lotus maintainers"]
description = "Parametric bit-level integer codec with tiered self-delimiting headers"
license = "Apache-2.0"
repository = "https://github.com/lotus-codec/lotus"
homepage = "https://github.com/lotus-codec/lotus"
documentation = "https://docs.rs/lotus"
keywords = ["codec", "compression", "integer", "bitstream", "varint"]
categories = ["compression", "encoding"]
readme = "README.md"
rust-version = "1.85"

[features]
default = []
small-int-fastpath = []
bigint = ["dep:num-bigint", "dep:num-traits"]
cli = ["dep:clap", "dep:hex", "dep:serde", "dep:serde_json"]

[dependencies]
clap = { version = "4.5", features = ["derive"], optional = true }
serde = { version = "1", features = ["derive"], optional = true }
serde_json = { version = "1", optional = true }
num-bigint = { version = "0.4", optional = true }
num-traits = { version = "0.2", optional = true }
hex = { version = "0.4", optional = true }
thiserror = "1.0"

[dev-dependencies]
criterion = "0.5"
proptest = "1.5"
rand = "0.8"

[[bench]]
name = "comparison"
harness = false

[[bin]]
name = "lotus"
path = "src/bin/lotus.rs"
required-features = ["cli"]

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
